# OpenAI API 密钥（必填）
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# 使用的模型（可选，默认 gpt-4o）
OPENAI_MODEL=gpt-4o

# 数据存储目录（可选，默认 ./data）
DATA_DIR=./data

# 应用基础 URL（生产环境必填）
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 免费用户每月对话次数限制（可选，默认 3）
FREE_SESSION_LIMIT=3
